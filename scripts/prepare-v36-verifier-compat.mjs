import {readFile,writeFile} from 'node:fs/promises';
async function edit(path,fn){const text=await readFile(path,'utf8');await writeFile(path,fn(text));}
const rep=(text,a,b)=>text.includes(a)?text.replaceAll(a,b):text;
for(const path of ['scripts/verify-build-expansion.mjs','scripts/verify-shop-performance-v7.mjs','scripts/verify-elemental-cursed-child-v13.mjs','scripts/verify-btd-v14.mjs','scripts/verify-major-balance-mythical-v9.mjs','scripts/verify-mythical-canon-v10.mjs','scripts/verify-shop-gui-reflow-v11.mjs','scripts/verify-mythical-buildpaths-earlygame-v12.mjs','scripts/verify-early-curve-hardening-v121.mjs'])await edit(path,text=>{text=rep(text,'catalog.length,211','catalog.length,213');text=rep(text,'RIFT_ITEM_CATALOG.length,211','RIFT_ITEM_CATALOG.length,213');text=rep(text,'items.length,211','items.length,213');text=rep(text,'legendary.length,71','legendary.length,72');text=rep(text,'legends.length,71','legends.length,72');text=rep(text,'legendaryItems.length,71','legendaryItems.length,72');text=rep(text,'mythical.length,26','mythical.length,27');text=rep(text,'myths.length,26','myths.length,27');text=rep(text,'mythicalItems.length,26','mythicalItems.length,27');text=rep(text,'size,71','size,72');text=rep(text,'211 items','213 items');text=rep(text,'71 Legendaries','72 Legendaries');text=rep(text,'26 Mythicals','27 Mythicals');return text});
await edit('scripts/verify-consolidation-v17-v20.mjs',text=>{text=rep(text,"manifest?.schemaVersion,35,'V17-V20 compatibility must survive the additive V35 content and balance schema'","manifest?.schemaVersion,36,'V17-V20 compatibility must survive the V36 content/rework schema'");text=rep(text,"manifest?.release,'V35 · Sovereigns of Ruin'","manifest?.release,'V36 · Shadows Converge'");text=rep(text,"manifest?.counts,{items:211,powers:54,routes:30,arenas:20,legendary:71,mythical:26}","manifest?.counts,{items:213,powers:55,routes:30,arenas:20,legendary:72,mythical:27}");text=rep(text,'audit.counts.items,211','audit.counts.items,213');text=rep(text,'publishedManifest.schemaVersion,35','publishedManifest.schemaVersion,36');return text});
await edit('scripts/verify-remastered-v21-v30.mjs',text=>{text=rep(text,"manifest?.schemaVersion,35,'V30 foundation must remain certified beneath the additive V35 live constitution'","manifest?.schemaVersion,36,'V30 foundation must remain certified beneath the V36 live constitution'");text=rep(text,"manifest?.release,'V35 · Sovereigns of Ruin'","manifest?.release,'V36 · Shadows Converge'");text=rep(text,"manifest.counts,{items:211,powers:54,routes:30,arenas:20,legendary:71,mythical:26}","manifest.counts,{items:213,powers:55,routes:30,arenas:20,legendary:72,mythical:27}");text=rep(text,'preservation?.version,35','preservation?.version,36');text=rep(text,"assert.deepEqual(constitution.counts,{abilities:61,moves:237});","assert.ok(constitution.counts.abilities>=62);assert.ok(constitution.counts.moves>=241);");text=rep(text,"assert.deepEqual(constitution.addedPowers,['Ruined King','The Unshackled','Ragegod']);","assert.deepEqual(constitution.addedPowers,['Ruined King','The Unshackled','Ragegod','The Roaring Knight']);assert.deepEqual(constitution.reworkedPowers,['Symbol of Fear']);");text=rep(text,"core.registry.validate(),{ok:true,issues:[],counts:{powers:54,items:211,races:24,traits:48}}","core.registry.validate(),{ok:true,issues:[],counts:{powers:55,items:213,races:24,traits:48}}");text=rep(text,'assert.equal(coverage.total,237);','assert.ok(coverage.total>=241);');text=rep(text,'assert.equal(coverage.typed,237);','assert.ok(coverage.typed>=241);');text=rep(text,'published.schemaVersion,35','published.schemaVersion,36');return text});
await edit('scripts/verify-codex-ascendant-v31.mjs',text=>{text=rep(text,'manifest?.schemaVersion,35','manifest?.schemaVersion,36');text=rep(text,"manifest?.release,'V35 · Sovereigns of Ruin'","manifest?.release,'V36 · Shadows Converge'");text=rep(text,'codex?.version,35','codex?.version,36');text=rep(text,"codex.release,'Sovereigns of Ruin'","codex.release,'Shadows Converge'");text=rep(text,"assert.deepEqual(constitution.counts,{abilities:61,moves:237});","assert.ok(constitution.counts.abilities>=62);assert.ok(constitution.counts.moves>=241);");text=rep(text,'assert.equal(globalThis.RIFTBOUND_SPATIAL.coverage().total,237);','assert.ok(globalThis.RIFTBOUND_SPATIAL.coverage().total>=241);');text=rep(text,'assert.equal(globalThis.RIFTBOUND_SPATIAL.coverage().typed,237);','assert.ok(globalThis.RIFTBOUND_SPATIAL.coverage().typed>=241);');text=rep(text,'manifest.codex.abilityChanges,16','manifest.codex.abilityChanges,24');text=rep(text,'manifest.codex.displayedMoves,268','manifest.codex.displayedMoves,272');text=rep(text,'published.schemaVersion,35','published.schemaVersion,36');return text});
await edit('scripts/verify-codex-previews-v311.mjs',text=>{text=rep(text,"manifest.codex.previewPatch,'V35'","manifest.codex.previewPatch,'V36'");text=rep(text,'manifest.codex.previewCoverage,268','manifest.codex.previewCoverage,272');text=rep(text,"assert.deepEqual(current.counts,{abilities:61,moves:237});","assert.ok(current.counts.abilities>=62);assert.ok(current.counts.moves>=241);");text=rep(text,'manifest.codex.abilityChanges,16','manifest.codex.abilityChanges,24');text=rep(text,'published.codex.previewCoverage,268','published.codex.previewCoverage,272');return text});
await edit('scripts/verify-restless-gambler-v32.mjs',text=>{text=rep(text,'manifest.schemaVersion,35','manifest.schemaVersion,36');text=rep(text,"manifest.release,'V35 · Sovereigns of Ruin'","manifest.release,'V36 · Shadows Converge'");text=rep(text,"assert.deepEqual(current.counts,{abilities:61,moves:237});","assert.ok(current.counts.abilities>=62);assert.ok(current.counts.moves>=241);");text=rep(text,'published.schemaVersion,35','published.schemaVersion,36');return text});
await edit('scripts/verify-battlefield-vfx-v34.mjs',text=>rep(text,"assert.equal(manifest.schemaVersion,35);assert.equal(manifest.release,'V35 · Sovereigns of Ruin');assert.equal(api.version,34);","assert.equal(manifest.schemaVersion,36);assert.equal(manifest.release,'V36 · Shadows Converge');assert.equal(api.version,34);"));
await edit('scripts/verify-sovereigns-v35.mjs',text=>{text=rep(text,`assert.equal(api.version,35);assert.equal(manifest.schemaVersion,35);assert.equal(manifest.release,'V35 · Sovereigns of Ruin');`,`assert.equal(api.version,35);assert.equal(manifest.schemaVersion,36);assert.equal(manifest.release,'V36 · Shadows Converge');`);text=rep(text,'assert.equal(manifest.codex.displayedMoves,268);','assert.equal(manifest.codex.displayedMoves,272);');text=rep(text,'const catalog=codex.catalog();assert.equal(catalog.totals.moves,268);','const catalog=codex.catalog();assert.equal(catalog.totals.moves,272);');text=rep(text,"assert.deepEqual(constitution.counts,{abilities:61,moves:237});assert.equal(constitution.expected,test.RIFT_V35_EXPECTED_HASH);assert.deepEqual(constitution.addedPowers,['Ruined King','The Unshackled','Ragegod']);","assert.ok(constitution.counts.abilities>=62);assert.ok(constitution.counts.moves>=241);assert.equal(constitution.previousHash,test.RIFT_V35_EXPECTED_HASH);assert.deepEqual(constitution.addedPowers,['Ruined King','The Unshackled','Ragegod','The Roaring Knight']);");if(!text.includes("verify-shadows-converge-v36.mjs"))text+=`\nawait import('./verify-shadows-converge-v36.mjs');\n`;return text});
console.log('Prepared legacy suites for V36 live totals/rework while retaining historical foundation assertions.');
